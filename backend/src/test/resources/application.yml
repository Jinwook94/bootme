spring:
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug

security:
  jwt:
    bootme:
      issuer:
      secret-key:
      exp:
        millisecond:
          access:
          refresh:
        second:
          access:
          refresh:
    google:
      issuer:
      audience:
    naver:
      issuer:
      audience:
    kakao:
      issuer:
      audience: